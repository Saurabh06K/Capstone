export interface Employee {
    id: number;  // Unique identifier for the employee
    accountNumber: string;  // Employee's account number
    email: string;  // Employee's email address
    firstName: string;  // Employee's first name
    lastName: string;  // Employee's last name
    salary: number;  // Employee's salary
    clientRegistrationNumber: string;  // Client registration number associated with the employee
    role: string;  // Employee's role
}
